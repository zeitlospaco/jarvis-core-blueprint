# Render.com Blueprint Configuration for n8n Service
# Region: Frankfurt (eu-central)
# HTTPS: Automatically enabled for all services

services:
  - type: web
    name: jarvis-n8n
    env: docker
    region: frankfurt
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    autoDeploy: true
    plan: standard

    # Start the n8n process explicitly
    startCommand: "n8n start"

    # Persistent disk for n8n data (workflows, credentials)
    disk:
      name: n8n-data
      mountPath: /home/node/.n8n
      sizeGB: 10

    envVars:
      - key: N8N_HOST
        value: 0.0.0.0
      - key: N8N_PORT
        value: 5678
      - key: N8N_PROTOCOL
        value: https
      - key: WEBHOOK_URL
        sync: false
      - key: N8N_EDITOR_BASE_URL
        sync: false

      # Authentication / encryption
      - key: N8N_BASIC_AUTH_ACTIVE
        value: true
      - key: N8N_BASIC_AUTH_USER
        sync: false
      - key: N8N_BASIC_AUTH_PASSWORD
        sync: false
      - key: N8N_ENCRYPTION_KEY
        sync: false

      # Database / Supabase
      - key: DB_TYPE
        value: postgresdb
      - key: DB_POSTGRESDB_HOST
        fromDatabase:
          name: jarvis-postgres
          property: host
      - key: DB_POSTGRESDB_PORT
        fromDatabase:
          name: jarvis-postgres
          property: port
      - key: DB_POSTGRESDB_DATABASE
        fromDatabase:
          name: jarvis-postgres
          property: database
      - key: DB_POSTGRESDB_USER
        fromDatabase:
          name: jarvis-postgres
          property: user
      - key: DB_POSTGRESDB_PASSWORD
        fromDatabase:
          name: jarvis-postgres
          property: password

      # Supabase variables (explicit names requested)
      - key: SUPABASE_DB_HOST
        sync: false
      - key: SUPABASE_SERVICE_ROLE
        sync: false
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false

      # AI / external keys
      - key: OPENAI_API_KEY
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: PINECONE_API_KEY
        sync: false
      - key: PINECONE_ENV
        value: us-east1-gcp

      # Other envs
      - key: LANGCHAIN_API_KEY
        sync: false
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        value: 587
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASSWORD
        sync: false
      - key: SMTP_FROM_EMAIL
        sync: false


databases:
  - name: jarvis-postgres
    databaseName: jarvis_n8n
    user: jarvis_admin
    region: frankfurt
    plan: starter